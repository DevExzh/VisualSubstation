import {InjectionKey} from "vue";

export const iconSizeKey: InjectionKey<string> = Symbol();
export const offsetKey: InjectionKey<number> = Symbol();
export const scaleFactorKey: InjectionKey<number> = Symbol();